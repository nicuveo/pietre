fn b1(x: bool) -> bool {
  return x;
}

fn b2(x: bool) -> bool {
  return !x;
}

fn b3(x: bool) -> bool {
  return !x;
}

fn b4(x: bool) -> bool {
  return x;
}

fn b6(x: bool) -> bool {
  return x;
}

fn b7(x: bool) -> bool {
  return !x;
}

fn b8(x: bool) -> bool {
  return !x;
}

fn b9(x: bool) -> bool {
  return x;
}
